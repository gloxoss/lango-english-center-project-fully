import { NextResponse } from 'next/server'; import { apiErrorResponse } from '@/libs/api/errors'; import { requireLibraryContext } from '@/features/library/api/guard'; import { libraryOverview } from '@/features/library/services/library-service'; export async function GET(request: Request) { try { const { tenantId } = await requireLibraryContext(request, 'library.report.read'); return NextResponse.json({ success: true, data: await libraryOverview(tenantId) }); } catch (e) { return apiErrorResponse(e); } }
