import { NextResponse } from 'next/server'; import { apiErrorResponse } from '@/libs/api/errors'; import { requireLibraryContext } from '@/features/library/api/guard'; import { listCharges } from '@/features/library/services/library-operations-service'; export async function GET(r:Request){try{const{tenantId}=await requireLibraryContext(r,'library.circulation.operate');return NextResponse.json({success:true,data:await listCharges(tenantId)});}catch(e){return apiErrorResponse(e)}}
