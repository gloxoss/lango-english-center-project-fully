import { requireServerPage } from '@/libs/api/page-guard';import { PayrollPayments } from '@/features/workforce/ui/payroll-workspace';export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;await requireServerPage(locale,{allowedRoles:['school_admin','accountant'],requiredCapability:'payroll.payment.prepare'});return <PayrollPayments/>}
