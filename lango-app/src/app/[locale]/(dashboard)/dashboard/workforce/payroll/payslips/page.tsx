import { requireServerPage } from '@/libs/api/page-guard';import { PayrollPayslips } from '@/features/workforce/ui/payroll-workspace';export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;await requireServerPage(locale,{allowedRoles:['school_admin','accountant'],requiredCapability:'payroll.sensitive.read'});return <PayrollPayslips/>}
