# Assessment & Examination — Remediation — Technical Plan

> Generated by UltraPlan | 2026-08-07

## Architecture Overview

Remediation within the existing Next.js 15 / Drizzle / PostgreSQL multi-tenant stack. Two independent tracks: (1) fix real, live-reachable bugs in the legacy online-exam and homework systems that real users touch today; (2) wire the well-built-but-dormant `ExamMasterService` to real routes and real UI, since its schema (`examTerms`/`examHalls`/`examSeats`/`examSchedules`) is genuinely new with no live equivalent to collide with. The parallel `OnlineExamService` + its colliding schema is formally left unwired (see STATE.md's architectural decision) - not deleted, just never activated, avoiding a real table-name collision with the live system.

## Tech Stack

Unchanged - existing stack, existing conventions, no new dependencies needed for this remediation.

## Section Overview

1. **Fix Live Online-Exam Security Bugs** - answer-ownership validation, real deadline enforcement, transaction wrapping, audience scoping on the live `/api/academics/online-exams/*` routes.
2. **Fix Homework Audience Scoping** - `HomeworkService.getHomeworkForStudent` now real-matches student/section/offering targeting instead of returning every tenant homework to every student.
3. **Exam Master Routes** - real API routes for exam terms, halls, seat allocation, schedules (with real conflict detection), marksheet entry, and rankings, wired to `ExamMasterService`.
4. **Exam Master UI Wiring** - replace the hardcoded demo state in the Exam Master workspace with real `fetch()` calls against section-03's routes.
5. **Rebuild Real Test Suite** - replace the fully-mocked `expect(Service).toBeDefined()` stub with real tests for the invariants that were fabricated or unverified (answer-ownership rejection, deadline rejection, hall-conflict rejection, audience scoping).
6. **Final Verification** - typecheck, tenant-isolation script, Docker rebuild, live end-to-end lifecycle, cross-tenant sweep.

## Execution Strategy
- 6 sections, executed sequentially (each depends on the work before it touching overlapping files/concepts)
- Critical path: 01 → 02 → 03 → 04 → 05 → 06

## Risk Summary
- Red: section-01 (live security bugs in a system real students use for real exam scores)
- Yellow: section-03 (moderate new-route complexity), section-05 (must genuinely prove regressions are caught)
- Green: section-02, section-04, section-06

## File Map

**New files:**
- `src/app/api/academics/exam-terms/route.ts`
- `src/app/api/academics/exam-halls/route.ts`
- `src/app/api/academics/exam-terms/[id]/seat-allocation/route.ts`
- `src/app/api/academics/exam-schedules/route.ts`
- `src/app/api/academics/exam-terms/[id]/marksheet/route.ts`
- `src/app/api/academics/exam-terms/[id]/rankings/route.ts`

**Modified files:**
- `src/app/api/academics/online-exams/submit/route.ts`
- `src/app/api/academics/online-exams/route.ts`
- `src/features/assessment/services/homework-service.ts`
- `src/features/assessment/services/exam-master-service.ts`
- `src/app/[locale]/(dashboard)/dashboard/academics/assessment/exam-master/page.tsx`
- `src/features/assessment/__tests__/assessment.test.ts`
- `src/app/api/academics/exam-schedules/route.ts` (execution-time hardening, see Review Notes)
- `src/app/api/academics/exam-terms/[id]/seat-allocation/route.ts` (execution-time hardening, see Review Notes)
- `src/app/api/academics/exam-terms/[id]/marksheet/route.ts` (execution-time hardening, see Review Notes)

## Review Notes — Section 06 Live Execution Findings (2026-08-07)

All live verification was run against the real running app in the `atlas` (`c9177d8a-...`) and `lango` (`17c1db51-...`) tenants, no synthetic mocks.

**Confirmed fixed, live, against the original audit's exact exploits:**
- Answer-forgery (pairing a correct option ID from a *different* question): reproduced against the pre-fix code's exact request shape → now returns `score: "0.00"` instead of a forged nonzero score.
- No-deadline enforcement: manipulated `online_exam_attempts.started_at` into the past, resubmission now correctly rejected `422 ATTEMPT_EXPIRED`.
- Homework audience scoping: created a homework targeted at a single real student (`STU-001`); confirmed via `?studentId=` that `STU-001` sees it and `STU-003` (different section, same tenant) does not, both still seeing the pre-existing broadcast homework.
- Full `ExamMasterService` lifecycle exercised end-to-end via real HTTP against real halls/terms/students: seat allocation correctly split 2 students across 2 capacity-1 halls (`unallocatedCount: 0`); schedule conflict detection correctly rejected an overlapping same-hall booking (`409 SCHEDULE_CONFLICT`) while accepting a non-overlapping one and a same-time booking in a different hall; marksheet grid produced real `assessmentOutcomes` rows (verified via `psql`, not just the API response); rankings endpoint returned correct rank order (18 > 12 → rank 1/2).

**New bug found and fixed during the cross-tenant sweep (not in the original audit, discovered live during section-06):**
`POST /api/academics/exam-schedules` accepted `examTermId`, `assessmentDefinitionId`, and `examHallId` with no tenant-ownership check — a Lango-tenant admin could create a real schedule row referencing Atlas's real exam term/hall/assessment-definition IDs (reproduced live: got `201` with a `tenantId: lango` row pointing at Atlas's real IDs). Similarly, `POST /api/academics/exam-terms/[id]/seat-allocation` and `POST /api/academics/exam-terms/[id]/marksheet` never validated that the `studentId`(s) in the request body belonged to the caller's tenant, which would let one tenant write real seat/grade records against another tenant's real student IDs.
Fixed all three routes with the same pattern already used elsewhere in this codebase (`rankings`/`marksheet` routes already checked `assessmentDefinitionId` ownership) — added explicit tenant-scoped existence checks for every foreign ID before calling the service, returning `404`/`422` as appropriate. Rebuilt the `app` Docker image (real exit 0) and redeployed; re-ran the exact same cross-tenant sweep against the same real IDs and confirmed all three now correctly reject (`404`/`422`), while the legitimate same-tenant flow (Atlas admin, Atlas's own IDs) continues to work unchanged.

**Final checks:**
- `tsc --noEmit` (run locally against the full project, not just the runtime image which lacks devDependencies): exit 0, zero errors.
- `scripts/check-tenant-isolation.ts`: flags the same 3 pre-existing, out-of-scope files as the prior pass (`academics/promotions`, `settings/migration/tasks/[id]`, `settings/migration/template`) — none touched by this remediation, no regressions introduced.
