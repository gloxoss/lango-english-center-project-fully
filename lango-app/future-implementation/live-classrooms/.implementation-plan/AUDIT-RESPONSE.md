# Live Classrooms — Audit Response (one row per finding)

Status legend: ✅ fixed + verified · 🟡 in progress / insufficiently tested · ❌ not started · ⛔ blocked by external provider
Updated: 2026-08-08 (P1-1, P1-2 completed this session)

| # | Finding | Disposition | Executable evidence |
|---|---|---|---|
| P0-1 | Durable single-use join grants (replay survives restart) | ✅ | Migration `0087_live_classrooms_hardening` (idx 88): `live_class_join_grants` stores SHA-256 nonce hash, unique redemption, tenant FK, partial expiry index. `redeemJoinGrant` = atomic `UPDATE … WHERE redeemed_at IS NULL RETURNING` (exactly one concurrent winner; others `JOIN_TOKEN_REPLAYED`). Durable across processes/restart. DB tests: concurrent redemption + replay. |
| P0-2 | Secure signing-key config (no known fallback secret) | ✅ | `providers/signing-key.ts`: production fails closed on missing/<32-char/known-insecure `LIVE_JOIN_SECRET`; dev/test use a labeled `dev:` secret. `GET /api/addons/live-classrooms/health` reports `{configured, mode}` — never the value. Secret never in DB/API/logs/bundles. Unit tests: dev/test/prod-missing/prod-short/prod-known-insecure. |
| P0-3 | Global release gates (tsc + next build exit 0) | 🟡 | Global `npx tsc --noEmit --pretty false` **exits 0** after the P1-1/P1-2 changes. `npx next build` pending — a concurrent agent has repeatedly held the `.next` lock; build will be re-run once the lock frees (see IMPLEMENTATION-REPORT §6). |
| P1-1 | Provider saga (no network I/O inside a DB tx) | ✅ | `session-service.ts` reserves state + persists `live_class_provider_operations` idempotency row in one short tx, calls the provider **outside** any transaction under a bounded timeout, persists outcome atomically. Atomic pending→running claim, stale-running-lease reclaim, loser-worker converge via bounded poll. Deterministic `scripted-provider` double proves: timeout→recoverable `failed`; retry reuses room; concurrent workers → 1 room; provider-ok-but-persist-failed → reclaimed; delayed provider → ok; cancel-during-create → valid `cancelled` + room cleanup. |
| P1-2 | Concurrency-safe scheduling (DB-authoritative) | ✅ | Migration `0091_live_classrooms_schedule_overlap` (idx 92): GiST EXCLUDE constraints over `tsrange(scheduled_start, scheduled_end, '[)')`, partial `WHERE status NOT IN ('cancelled','ended')`, tenant-scoped, `btree_gist` enabled; applied to live DB (both constraints present, `\d` verified; 0 pre-existing conflicts). Service maps `SQLSTATE 23P01` (incl. `.cause` wrapper) → `409 LIVE_SESSION_CONFLICT`. 11 DB tests: concurrent identical raw inserts (1 wins / 23P01), concurrent `createLiveSession` (1×409), teacher across classes, class two teachers, adjacent OK, cancelled/ended free slot / failed blocks, UPDATE-caused overlap, friendly 409, DST boundary, deterministic TOCTOU race → 409, cross-tenant OK. |
| P1-3 | Effective guardian authorization | ❌ | Not started. Plan: read authoritative guardian entity + relationship, enforce active/effective-dates/custody/per-child/relationshipType. |
| P1-4 | Webhook provider binding | ❌ | Not started. Plan: resolve by provider_type+profile+meeting_id+tenant, per-profile secret, rate-limit, payload cap, transactional idempotent receipt. |
| P1-5 | Attendance calculations + dedicated tests | ❌ | Not started. Plan: `attendance-service.test.ts` for interval-union/reconnect/grace/threshold + versioned upsert. |
| P1-6 | Core attendance posting | ❌ | Not started. Plan: authoritative register path, immutable source marker, idempotency key, revision/reversal. |
| P1-7 | Lifecycle races | ❌ | Not started. Plan: barrier/hook harness for start/start, start/end, start/cancel, end/end, sync/end, provider-callback-during-create, expiry-sweep/start, recording sync/delete. |
| P1-8 | Harden provider adapters | 🟡 | `external_link` join tokens still need HTTPS-only URL validation + SSRF/timeout hardening. BBB stays disabled/uncertified. |
| P1-9 | Authenticated HTTP adversarial suite | ❌ | Not started. Plan: 23 route handlers; anonymous→401, addon-disabled→403, missing capability→403, wrong role→403, cross-tenant→404/403, teacher scope→denied, malformed→400/422, valid→expected. A skipped suite is not a pass. |
| P1-10 | Branch / relationship scope | ✅ | Live-classrooms is tenant-wide by policy; no `branchId` on sessions; client-supplied branch id is never accepted as authorization. Documented in PROVIDER-ADAPTERS.md / IMPLEMENTATION-REPORT.md. |
| P1-11 | Migration hardening | 🟡 | timezone writes fixed to naive-local (`toLocalNaive`); `actualStart`/`actualEnd` corrected. Composite-tenant FK hardening + preflight violation detection are scheduled with migration finalization (P1-11 still open). |
| P2 UI / accessibility | Design-system completion | 🟡 | Manual sweep items open (touch targets, focus-visible, SR errors, duplicate-submit, accessible dialogs, RTL, state differentiation). |
| P2 Performance / ops | Bounded payloads, timeouts, retention, polling | 🟡 | Provider calls bounded (P1-1); retention covers stale sessions + expired join grants; pagination/payload caps/polling-cancellation verification open. |

**Completion rule (binding):** do not claim "fully implemented and verified" until every P0/P1 row above is ✅ with passing evidence, global tsc exits 0, production build exits 0, browser/manual acceptance is complete, and every remaining external-provider limitation is disclosed in PROVIDER-ADAPTERS.md.
