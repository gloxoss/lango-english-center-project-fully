{
  "_README": "Ratchet baselines for scripts/check-ui-reality.ts. Each count may only go DOWN. Lower a number when you fix something, so the gain is locked in. Raising one is allowed but must be deliberate and explained in the PR - it means shipping a control that does nothing, a screen with invented data, a page nobody can reach, or dead code.",
  "_measured": "2026-09-23, five imported-fixture Communication routes archived after the deep visual sweep.",
  "deadControls": 38,
  "_deadControls": "A <Button>/<button> with no onClick, submit, link or trigger parent. Lowered to 47 after entitlements module ticket modal and license certificate actions wired.",
  "mockScreens": 0,
  "_mockScreens": "0 current findings, including the imported data fixture state pattern. The heuristic does not prove that every fetch returns real records.",
  "unlinkedPages": 31,
  "_unlinkedPages": "Static dashboard pages with no sidebar or portal-manifest entry points. Five direct-URL mock Communication routes were archived. Some remaining pages are reached from a parent screen by design. RAISED 28 -> 31 deliberately (IMPL-ATTENDANCE-REFORM-01): /dashboard/attendance/suivi, /dashboard/cards/badges and /dashboard/attendance/registres are built and browser-verified, but their sidebar entries land in the campaign's single navigation-consolidation pass, which is deferred so that several features do not each rewrite the same shared sidebar.tsx block. The entries are owed, not forgotten. Lower back to 28 in the navigation pass - if it does not come back down, a page really is unreachable.",
  "orphanedComponents": 7,
  "_orphanedComponents": "Exported components nothing in src imports. Was 14; seven mock/duplicate screens were moved to future-implementation/_archived-ui. The remaining seven are small prop-driven building blocks, not fake screens."
}
