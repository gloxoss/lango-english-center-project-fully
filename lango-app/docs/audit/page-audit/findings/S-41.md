# S-41 · Auth rate limit counts every page's session check, per IP

**Severity: P1** · Source report: [2026-09-23-seeded-db-role-and-addon-audit.md](../../2026-09-23-seeded-db-role-and-addon-audit.md) · Pages affected: 0 (plus cross-cutting on every page for roles: *)

**Code check (2026-09-23):** STILL OPEN: no `/get-session` rule in `libs/auth.ts` rateLimit.

## What is wrong

One admin browsing alone got 429 on `/api/auth/get-session` on several pages.

## Why

`rateLimit: { window: 60, max: 100 }` in `libs/auth.ts:65` applies to all `/api/auth/*`; schools share one public IP.

## Fix

Exempt or raise `/get-session`; keep strict limits on `/sign-in/*` and 2FA.

## Plan

1. Add a customRule for `/get-session`.
2. Load test: 200 page loads in 2 minutes from one IP.

## Done when

No 429 on get-session under normal classroom load.

## Pages

No page matched (cross-cutting, or the page no longer exists).