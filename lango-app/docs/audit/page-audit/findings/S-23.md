# S-23 · Audit log shows internal names and writes rows on view

<!-- status -->
> **Status (2026-09-25): PARTIAL.** Rejected by owner-invalidate: VERIFICATION VOIDED on the owner's explicit instruction, not a code rejection. The earlier ok came from verifier-1/verifier-2, identities run by gemini-audit itself (76 checks in 15 min, notes pre-written in scratch/verify_all_batches.mjs, some describing code that does not exist, e.g. S-45 salary-advance-service.ts). Recorded by claude-finance as owner-invalidate, 2026-09-25. Needs a real independent re-verify: re-run the done --verify command and quote file:line.
<!-- /status -->

**Severity: P2** · Source report: [2026-09-23-deep-visual-sweep.md](../../2026-09-23-deep-visual-sweep.md) · Pages affected: 1

**Code check (2026-09-23):** Not re-checked since the sweep.

## What is wrong

Internal module names and raw UUIDs; opening reconciliation writes an "export" row each view; replayed enrolment writes a second create row.

## Why

Raw entity names; audit on GET; replay not idempotent in audit.

## Fix

Human labels; no audit on plain views; skip audit on idempotent replay.

## Plan

1. Label map.
2. Remove GET audit.
3. Guard replay.

## Done when

One row per real action with readable labels.

## Pages

- [`/dashboard/settings/audit-logs`](../pages/settings/settings__audit-logs.md) · NEEDS FIX (P2)

## Screenshots

`/dashboard/settings/audit-logs` · Pass A · school_admin

![](../shots/A__school_admin-fr-settings__audit-logs.jpg)
