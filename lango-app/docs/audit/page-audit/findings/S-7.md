# S-7 · ~1 375 hardcoded French UI strings bypass translation (Arabic UI stays French)

**Severity: P1** · Source report: [2026-09-23-deep-visual-sweep.md](../../2026-09-23-deep-visual-sweep.md) · Pages affected: 0 (plus cross-cutting on every page for roles: *)

**Code check (2026-09-23):** Not re-checked since the sweep.

## What is wrong

Accented strings only (real count higher). Top: settings 318, students 205, crm 115, events 78, super-admin 75, academics 66.

## Why

Strings written directly in JSX instead of `t()`.

## Fix

Move strings to message files module by module, starting with the most used screens.

## Plan

1. Extend the i18n checker to count hardcoded accented strings per module (ratchet).
2. Migrate settings and students first.
3. Arabic sweep of the migrated pages.

## Done when

Ratchet count goes down every PR; migrated pages render fully in Arabic.

## Pages

No page matched (cross-cutting, or the page no longer exists).