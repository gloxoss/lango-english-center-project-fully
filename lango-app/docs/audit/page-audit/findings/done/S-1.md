# S-1 · Five Communication pages ran entirely on invented data

<!-- status -->
> **Status (2026-09-23): DONE.** claude-1 fixed; verified by a second agent in the Agent Hub (claude-auditor): 5 routes deleted, ui-reality tests 44 passed.
<!-- /status -->


**Severity: P0** · Source report: [2026-09-23-deep-visual-sweep.md](../../../2026-09-23-deep-visual-sweep.md) · Pages affected: 0

**Code check (2026-09-23):** LIKELY FIXED: the 5 routes no longer exist in the app.

## What is wrong

`leads`: "Nouveaux leads 128 · +18 %", 16 named prospects dated May 2025, contact `prospect@schoolos.ma`. No fetch in the client.

## Why

Clients seed state from `features/crm/data/*` config modules and hardcode KPIs (`lead-pipeline-client.tsx:184-217`). `check:ui` only detects arrays declared in the same file.

## Fix

Archive the 5 pages to `future-implementation/_archived-ui`. Extend `scripts/check-ui-reality.ts` to flag a client with no fetch that seeds state from an imported `data/*` module.

## Plan

1. Confirm the 5 routes return 404 (they are no longer in the page list as of 2026-09-23, so likely already archived).
2. Extend the `check:ui` ratchet as described.
3. Run `check:ui` and confirm 0 mock screens.

## Done when

Routes 404; `check:ui` catches an imported-data mock in a test fixture.

## Pages

No page matched (cross-cutting, or the page no longer exists).

Routes named in the finding that no longer exist in the app (verify they were removed on purpose): `/dashboard/communication/leads`, `/dashboard/communication/segments`, `/dashboard/communication/forms`, `/dashboard/communication/milestones`, `/dashboard/communication/templates-automation`